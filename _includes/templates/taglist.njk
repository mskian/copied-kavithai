{% for post in postslist %}
<h2 style="font-size: 15px;" class="home-title">
<a href="{{ post.url }}" {% if entry.url == url %} aria-current="page"{% endif %}>
{% if post.title %}
{{ post.title }}
 {% else %}
{{ post.url }}
{% endif %}
</a>
</h2> 
<p style="font-size: 10px;" class="post-date">
<time class="postList-date" datetime="{{ post.updated_date | date('DATE') }}">
{{ post.updated_date | date('HUMAN_DATE') }}
</time>
{% for tag in post.tags %}
{% set page = search.page("type=tag tag='" + tag + "'") %}
{% if page %}
<a style="font-size: 10px;" href="{{ page.url  }}" class="tag is-info">{{ tag }}</a>
{% endif %}
{% endfor %}
</p>
<hr>
{% endfor %}